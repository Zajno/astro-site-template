---
    import './style.scss';
    import { Locales, ModalMenuCopyright } from "sitemap/copyright";

    type Props = {
        locale: Locales
    }

    const { locale } = Astro.props;

    const Strings = ModalMenuCopyright[locale];

    // const getLocaleHref = (lang) => Sitemap.getLocaleHref(page, lang);
---


<div id="mobile-menu" class="mobile-menu">
    <!-- mobile menu markup goes here -->
</div>
