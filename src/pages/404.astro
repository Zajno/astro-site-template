---
import PageLayot from 'layouts/page.astro';
import { Page404 as PageSettings } from 'data/pages';

import { type Locales } from 'data/copyright';
import MainSection from 'sections/page404/main/view.astro';

type Props = {
	locale: Locales
};

const { locale = 'en' } = Astro.props;

---

<PageLayot
    {...PageSettings}
    locale={locale}
    isActiveFooter={false}
    isActiveMobileMenu={false}
    isActivePreloader={false}
>
    <MainSection />
</PageLayot>
