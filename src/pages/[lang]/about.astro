---
import { type Locales, createStaticPathGetter } from 'sitemap/copyright/types';
import AboutPage from '../about.astro';

export const getStaticPaths = createStaticPathGetter();

const { lang } = Astro.params;

---

<AboutPage locale={lang as Locales} />
