---
import { type Locales, createStaticPathGetter } from 'data/copyright/types';
import IndexPage from '../index.astro';

export const getStaticPaths = createStaticPathGetter();

const { lang } = Astro.params;

---

<IndexPage locale={lang as Locales} />
