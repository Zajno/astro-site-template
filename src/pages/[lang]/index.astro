---
import { Locales } from 'sitemap/copyright/types';
import IndexPage from '../index.astro';

export function getStaticPaths() {
  return [
    {params: {lang: 'en'}},
    {params: {lang: 'ja'}}
  ]
}

const {lang} = Astro.params;

---

<IndexPage locale={lang as Locales} />